
name: fitness_pro
description: "Smart gym management — members, trainers, payments & attendance in one place."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.0.0
  dio: ^5.7.0
  get_it: ^8.0.0
  json_annotation: ^4.9.0
  freezed_annotation: ^2.4.1
  shared_preferences: ^2.3.0
  google_fonts: ^6.2.1
  go_router: ^14.0.0
  flutter_secure_storage: ^9.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  freezed: ^2.5.0
  json_serializable: ^6.8.0
  build_runner: ^2.4.0

flutter:
  assets:
#    - lib/Asset/yoga_photo.jpg
#    - lib/Asset/fitness_logo.png
  uses-material-design: true
