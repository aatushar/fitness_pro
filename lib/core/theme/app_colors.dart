import 'package:flutter/material.dart';

class AppColors {
  // ── Dark mode ──────────────────────────────
  static const darkBg         = Color(0xFF0A0A12);
  static const darkSurface    = Color(0xFF12121E);
  static const darkSurfaceAlt = Color(0xFF1A1A2E);
  static const darkBorder     = Color(0xFF1E1E32);
  static const darkBorderAct  = Color(0xFF7B2FBE);
  static const darkTextPrim   = Color(0xFFFFFFFF);
  static const darkTextMuted  = Color(0xFF5A5A7A);
  static const darkLabel      = Color(0xFF7B2FBE);
  static const darkStrFill    = Color(0xFF2A2A3A);

  // ── Light mode ─────────────────────────────
  static const lightBg        = Color(0xFFF4F3FA);
  static const lightSurface   = Color(0xFFFFFFFF);
  static const lightSurfaceAlt= Color(0xFFEEECF8);
  static const lightBorder    = Color(0xFFDDDAF0);
  static const lightBorderAct = Color(0xFF7B2FBE);
  static const lightTextPrim  = Color(0xFF0D0820);
  static const lightTextMuted = Color(0xFF9490B0);
  static const lightLabel     = Color(0xFF7B2FBE);
  static const lightStrFill   = Color(0xFFE0DCF5);

  // ── Shared ─────────────────────────────────
  static const purple         = Color(0xFF7B2FBE);
  static const purpleDark     = Color(0xFF5A1E9A);
  static const purpleGlow     = Color(0x407B2FBE);
}